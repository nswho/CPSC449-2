order(?A).

family(?A).

genus(?A).

species(?A).

hasParent(?A, ?B).

hasCommonName(?N, ?C).

hasCommonName(?G, ?S, ?C).

hasSciName(?C, ?N).

hasCompoundName(?G, ?S, ?N).

isaStrict(?A, ?B).

isa(?A, ?B).

synonym(?A, ?B).

countSpecies(?A, -N).

rangesTo(?A, ?P).

habitat(?A, ?B).

food(?A, ?B).

nesting(?A, ?B).

behavior(?A, ?B).

conservation(?A, ?B).

